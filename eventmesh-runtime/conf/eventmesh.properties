# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

#
# EventMesh unified runtime configuration.
#
# Loaded from the classpath (conf/) by EventMeshApplication and passed to the storage plugin's
# init(). -D system properties (set by bin/start.sh or JVM flags) override these values.
#
# Storage backend — set EVENTMESH_STORAGE_TYPE (or -Deventmesh.storage.type) to one of:
#   rocketmq  — RocketMQ 4.x (remoting direct connect, classic PULL)
#   rocketmq5 — RocketMQ 5.x (remoting direct connect, 5.x POP + Lite Topic)
#   kafka     — Kafka (kafka-clients, assign+seek+poll, no consumer group, SASL support)
#
# Uncomment the section matching your backend and set the broker address.
#

# ===== RocketMQ 4.x (EVENTMESH_STORAGE_TYPE=rocketmq) =====
#eventMesh.server.rocketmq.namesrvAddr=127.0.0.1:9876
#eventMesh.server.rocketmq.cluster=DefaultCluster

# ===== RocketMQ 5.x (EVENTMESH_STORAGE_TYPE=rocketmq5) =====
#eventMesh.server.rocketmq5.namesrvAddr=127.0.0.1:9876
#eventMesh.server.rocketmq5.cluster=DefaultCluster
# Lite-topic pull offset checkpoint interval (ms). Offsets are persisted to disk periodically so a
# JVM crash replays at most this interval's worth of messages. Shutdown always persists regardless.
# Set to 0 or negative to disable periodic checkpointing (only persist on shutdown).
#eventmesh.rocketmq5.lite.checkpoint.interval.ms=5000

# ===== Kafka (EVENTMESH_STORAGE_TYPE=kafka) =====
eventMesh.server.kafka.namesrvAddr=localhost:9092
# Kafka SASL/SSL — uncomment for secured clusters (e.g. wemq-kafka). See kafka-client.properties
# for the full set of security.* / sasl.* / ssl.* keys (transparently passed to kafka-clients).
#security.protocol=SASL_PLAINTEXT
#sasl.mechanism=PLAIN
#sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username="USER" password="PASS";

# ===== Runtime ports (usually set via -D by bin/start.sh) =====
#eventmesh.http.port=8080
#eventmesh.admin.port=8081
#eventmesh.ws.port=-1
#eventmesh.offset.path=./data/offset



# ===== Delivery topology (uni runtime, issue #5293 / #5338) =====
# Selects how the runtime polls / dispatches events. Two modes:
#   LOCAL_STICKY_PULL      - single-instance / low-throughput default. Every runtime polls
#                             every partition of every subscribed topic. No Meta store
#                             dependency. Backward compatible with pre-#5301 deployments.
#   PARTITION_OWNED_PULL   - multi-instance scale-out. Each instance acquires ownership of
#                             a strict subset of partitions via Meta CAS + fencing. No
#                             duplicate consumption across instances. Requires a Meta store
#                             (Nacos / etcd / ZK). See #5309 for the wiring follow-up.
#
# Default: LOCAL_STICKY_PULL (matches the 6-arg UniRuntime constructor for backward
# compatibility). Set EVENTMESH_DELIVERY_TOPOLOGY (-Deventmesh.delivery.topology) to
# override. Missing / blank -> LOCAL_STICKY_PULL (graceful). Unknown value -> fail-fast
# IllegalArgumentException on startup (a typo must not silently degrade to single-instance).
#eventmesh.delivery.topology=LOCAL_STICKY_PULL

# ===== Connector Runtime (Experimental — see docs/eventmesh-features.md) =====
# Connectors run as an independent process (eventmesh-connector-runtime) that bridges external
# systems and EventMesh over CloudEvents/HTTP. This section is read by that process, not by the
# EventMesh server itself.
# Connector worker admin/metrics port on the connector-runtime process:
#eventmesh.connector.admin.port=8090
# Long-poll interval for the sink loop (ms):
#eventmesh.connector.poll.interval.ms=1000
# Max events pulled per sink batch:
#eventmesh.connector.sink.max.batch=100
# Offset storage backend for the connector process: memory | rocksdb | remote
#eventmesh.connector.offset.store=memory
# Offset persistence path when offset.store=rocksdb:
#eventmesh.connector.offset.path=./data/connector-offsets
